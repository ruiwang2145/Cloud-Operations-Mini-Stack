[pytest]
DJANGO_SETTINGS_MODULE = my_cloudapp.settings_test
python_files = tests.py test_*.py *_tests.py
testpaths = ops tasks
addopts =
    --tb=short
    --strict-markers
    -p no:randomly
markers =
    slow: tests that take noticeably longer than the rest
